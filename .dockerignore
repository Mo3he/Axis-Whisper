# The variant builds from the repository root (docker build -f aarch64/Dockerfile .)
# so keep the context lean. Do NOT exclude common/app/ or aarch64/app/ — the
# Dockerfile COPYs those.
.git
.github
.DS_Store
*.eap
build
releases
README.md
